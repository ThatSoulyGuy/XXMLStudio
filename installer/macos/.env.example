# XXMLStudio macOS Build Configuration
# Copy this file to .env.local and fill in your values
# .env.local is gitignored and will not be committed

# =============================================================================
# Qt Configuration
# =============================================================================

# Path to Qt installation (required)
QT_DIR="${HOME}/Qt/6.10.1/macos"

# =============================================================================
# Code Signing (optional)
# =============================================================================

# Developer ID Application certificate name (from Keychain)
# Find yours with: security find-identity -v -p codesigning
# Example: "Developer ID Application: Your Name (XXXXXXXXXX)"
DEVELOPER_ID=""

# =============================================================================
# Notarization (optional, requires code signing)
# =============================================================================

# Apple ID email used for App Store Connect
APPLE_ID=""

# App-specific password for notarization
# Generate at: https://appleid.apple.com/account/manage -> App-Specific Passwords
APPLE_PASSWORD=""

# Team ID (10-character string)
# Find yours at: https://developer.apple.com/account -> Membership
TEAM_ID=""

# =============================================================================
# Build Options
# =============================================================================

# Version string for the installer
VERSION="0.1.0"

# Set to 1 to skip code signing even if DEVELOPER_ID is set
SKIP_SIGNING=0

# Set to 1 to skip notarization even if credentials are set
SKIP_NOTARIZATION=0
